# sd15-flux2vae

create_base_model.py is a script that will pull in the sd1.5 model,
tweak it a little, swap in the flux2 vae, and save the results
to "sd-flux2-alpha00"

This result produces garbage, but it is ready to be retrained
